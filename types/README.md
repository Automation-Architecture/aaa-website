# Types

TypeScript type declarations for ambient modules and globals.

| File | Description |
|------|-------------|
| `gtm.d.ts` | Extends `Window` interface with `dataLayer` for Google Tag Manager. |
